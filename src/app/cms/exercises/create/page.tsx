"use client";

// import { ExerciseFormData } from "@/components/UI/dashboard/forms/defs/exercise.defs";
import Form from "@/components/UI/dashboard/form";
// import { createExercise } from "@/utils/api/exercises";
import { useRouter } from "next/navigation";

export default function CreateExercisePage() {
  const router = useRouter();

//   const handleSubmit = async (data: ExerciseFormData) => {
//     await createExercise(data);
//     router.push("/cms/exercises");
//   };

//   return <Form title="Criar Novo Exercício" onSubmit={} />;
}   
